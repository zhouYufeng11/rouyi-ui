import request from 'utils/request'

// 获取后端，表单信息，目的是呈现数据
export function getProView(query) {
  console.log(query);
  return;

    // return request({
    //   url: '/dzda/manage/list',
    //   method: 'get',
    //   params: query
    // })
  }